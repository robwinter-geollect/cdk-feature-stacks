// eslint-disable-next-line import/no-extraneous-dependencies
import { EnvConfig } from '../config/environment-config';

export const testEnvConfig: EnvConfig = {
  pipelines: {
    codestarConnectionId: 'test',
  },
};
